# Include database and OpenAI properties when running locally
#spring.config.import=optional:classpath:database.properties, optional:classpath:openai.properties

# Include database and OpenAI properties when running on Heroku
spring.datasource.url=${AzureDB_URL}
spring.datasource.username=${AzureDB_Username}
spring.datasource.password=${AzureDB_Password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

openai.model=${OpenAI_Model}
openai.api.url=${OpenAI_URL}
openai.api.key=${OpenAI_Key}